# Copy this file to docker-compose.override.yml if you need custom configuration
# This file is useful when you have port conflicts (e.g., local PostgreSQL running)

services:
  db:
    ports:
      # Change the host port if 5432 is already in use
      - "5433:5432"
